import { ItemViewDirective } from './item-view.directive';

describe('ItemViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemViewDirective();
    expect(directive).toBeTruthy();
  });
});
